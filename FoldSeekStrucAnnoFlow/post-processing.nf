#!/usr/bin/env nextflow

nextflow.enable.dsl = 2


workflow {
    // 1. match the names using the match_names.py script
    // 2. Next I want to use the mapping from the names to do the Interpro scan results to the pipeline

}